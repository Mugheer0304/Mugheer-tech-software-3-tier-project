# Outputs for the dns_cert module are declared in main.tf (certificate_arn,
# zone_id, app_fqdn, api_fqdn). This file intentionally left as a pointer to
# keep the standard module file layout (spec Section 20.3).
